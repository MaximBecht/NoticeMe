<Page
    x:Class="NoticeMe.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NoticeMe"
    xmlns:vm="using:NoticeMe.Data.ViewModels"
    xmlns:m="using:NoticeMe.Data.DataModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"    
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{x:Bind MainViewModel}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition MaxHeight="64"/>
        </Grid.RowDefinitions>

        <StackPanel x:Name="AppTopBar" VerticalAlignment="Bottom" FlowDirection="LeftToRight" Orientation="Horizontal">
            <!--Top Bar, can later be used, if needed-->
        </StackPanel>

        <Frame Grid.Row="1" x:Name="ContentFrame"/>

        <Rectangle  Grid.Row="2" Fill="#18181B" Opacity="0.5">
            
        </Rectangle>

        <StackPanel x:Name="AppNavigationBar" 
                    Grid.Row="2" 
                    HorizontalAlignment="Center"
                    FlowDirection="LeftToRight" 
                    Orientation="Horizontal"
                    CornerRadius="32"
                    Height="56">

            <Button Width="48" Height="48" Background="Transparent" BorderThickness="0" Margin="4, 0, 8, 0" CornerRadius="32">
                <BitmapIcon Width="32" Height="32" UriSource="ms-appx:///Assets/Images/icons8_leaderboard_32px.png" Foreground="White"/>
            </Button>
            <Button  Width="48" Height="48" Background="Transparent" BorderThickness="0" Margin="8, 0, 8, 0" CornerRadius="32">
                <BitmapIcon Width="32" Height="32" UriSource="ms-appx:///Assets/Images/icons8_male_user_32px.png" Foreground="White"/>
            </Button>
            <Button  Width="56" Height="56" Background="Transparent" BorderThickness="0" Margin="8, 0, 8, 0" CornerRadius="16">
                <BitmapIcon Width="48" Height="48" UriSource="ms-appx:///Assets/Images/icons8_home_48px.png" Foreground="White"/>
            </Button>
            <Button  Width="48" Height="48" Background="Transparent" BorderThickness="0" Margin="8, 0, 8, 0" CornerRadius="32">
                <BitmapIcon Width="32" Height="32" UriSource="ms-appx:///Assets/Images/icons8_settings_32px.png" Foreground="White"/>
            </Button>
            <Button  Width="48" Height="48" Background="Transparent" BorderThickness="0" Margin="8, 0, 4, 0" CornerRadius="32">
                <BitmapIcon Width="32" Height="32" UriSource="ms-appx:///Assets/Images/icons8_about_32px.png" Foreground="White"/>
            </Button>
            <StackPanel.Background>
                <SolidColorBrush Color="#6202EE" Opacity="1"/>
            </StackPanel.Background>
        </StackPanel>
    </Grid>
</Page>
